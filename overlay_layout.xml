package com.didicalc.app

import android.app.Notification
import android.app.NotificationChannel
import android.app.NotificationManager
import android.app.Service
import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent
import android.content.IntentFilter
import android.graphics.Color
import android.graphics.PixelFormat
import android.os.IBinder
import android.util.TypedValue
import android.view.Gravity
import android.view.LayoutInflater
import android.view.MotionEvent
import android.view.View
import android.view.WindowManager
import android.widget.TextView
import androidx.core.app.NotificationCompat
import kotlin.math.abs
import kotlin.math.roundToInt

class OverlayService : Service() {

    companion object {
        const val ACTION_UPDATE = "com.didicalc.app.OVERLAY_UPDATE"
        const val EXTRA_PXKM     = "pxkm"
        const val EXTRA_KM_PICKUP = "km_pickup"
        const val EXTRA_KM_TRIP   = "km_trip"
        const val EXTRA_PRICE     = "price"
        const val CHANNEL_ID      = "didi_calc_overlay"
    }

    private lateinit var windowManager: WindowManager
    private lateinit var overlayView: View
    private lateinit var params: WindowManager.LayoutParams

    // Views del overlay
    private lateinit var tvPxkm: TextView
    private lateinit var tvVerdict: TextView
    private lateinit var tvDetails: TextView
    private lateinit var tvWaiting: TextView
    private lateinit var contentGroup: View

    // Para arrastrar el overlay
    private var initialX = 0; private var initialY = 0
    private var initialTouchX = 0f; private var initialTouchY = 0f
    private var isDragging = false

    // Estado colapsado/expandido
    private var isCollapsed = false

    private val updateReceiver = object : BroadcastReceiver() {
        override fun onReceive(context: Context?, intent: Intent?) {
            intent ?: return
            if (intent.hasExtra(EXTRA_PXKM)) {
                val pxkm    = intent.getDoubleExtra(EXTRA_PXKM, 0.0)
                val kmPickup= intent.getDoubleExtra(EXTRA_KM_PICKUP, 0.0)
                val kmTrip  = intent.getDoubleExtra(EXTRA_KM_TRIP, 0.0)
                val price   = intent.getDoubleExtra(EXTRA_PRICE, 0.0)
                updateUI(pxkm, kmPickup, kmTrip, price)
            } else {
                showWaiting()
            }
        }
    }

    override fun onCreate() {
        super.onCreate()
        createNotificationChannel()
        startForeground(1, buildNotification())

        windowManager = getSystemService(WINDOW_SERVICE) as WindowManager
        setupOverlay()
        registerReceiver(updateReceiver, IntentFilter(ACTION_UPDATE),
            RECEIVER_NOT_EXPORTED)
    }

    private fun setupOverlay() {
        overlayView = LayoutInflater.from(this).inflate(R.layout.overlay_layout, null)

        tvPxkm    = overlayView.findViewById(R.id.tv_pxkm)
        tvVerdict = overlayView.findViewById(R.id.tv_verdict)
        tvDetails = overlayView.findViewById(R.id.tv_details)
        tvWaiting = overlayView.findViewById(R.id.tv_waiting)
        contentGroup = overlayView.findViewById(R.id.content_group)

        params = WindowManager.LayoutParams(
            dpToPx(200),
            WindowManager.LayoutParams.WRAP_CONTENT,
            WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,
            WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE or
                WindowManager.LayoutParams.FLAG_LAYOUT_IN_SCREEN,
            PixelFormat.TRANSLUCENT
        ).apply {
            gravity = Gravity.TOP or Gravity.END
            x = dpToPx(8)
            y = dpToPx(80)
        }

        // Tocar para colapsar/expandir; arrastrar para mover
        overlayView.setOnTouchListener { _, event ->
            when (event.action) {
                MotionEvent.ACTION_DOWN -> {
                    initialX = params.x; initialY = params.y
                    initialTouchX = event.rawX; initialTouchY = event.rawY
                    isDragging = false
                    true
                }
                MotionEvent.ACTION_MOVE -> {
                    val dx = event.rawX - initialTouchX
                    val dy = event.rawY - initialTouchY
                    if (!isDragging && (abs(dx) > 8 || abs(dy) > 8)) isDragging = true
                    if (isDragging) {
                        params.x = (initialX - dx).toInt()
                        params.y = (initialY + dy).toInt()
                        windowManager.updateViewLayout(overlayView, params)
                    }
                    true
                }
                MotionEvent.ACTION_UP -> {
                    if (!isDragging) toggleCollapse()
                    true
                }
                else -> false
            }
        }

        showWaiting()
        windowManager.addView(overlayView, params)
    }

    private fun toggleCollapse() {
        isCollapsed = !isCollapsed
        if (isCollapsed) {
            contentGroup.visibility = View.GONE
            tvWaiting.visibility = View.GONE
            params.width = dpToPx(60)
        } else {
            params.width = dpToPx(200)
            // Volver al estado anterior
            if (tvPxkm.text.isNotEmpty() && tvPxkm.text != "") {
                contentGroup.visibility = View.VISIBLE
                tvWaiting.visibility = View.GONE
            } else {
                showWaiting()
            }
        }
        windowManager.updateViewLayout(overlayView, params)
    }

    private fun updateUI(pxkm: Double, kmPickup: Double, kmTrip: Double, price: Double) {
        val pxkmRounded = pxkm.roundToInt()
        val totalKm = kmPickup + kmTrip
        val isLong = kmTrip >= DiDiAccessibilityService.LONG_TRIP_KM

        tvPxkm.text = "$$pxkmRounded"

        // Veredicto y color
        val (verdictText, bgColor, textColor) = when {
            pxkm >= 700 -> Triple("★ Excelente", Color.parseColor("#14532d"), Color.parseColor("#4ade80"))
            pxkm >= DiDiAccessibilityService.MIN_PRICE_PER_KM ->
                Triple("✓ Acepta", Color.parseColor("#14532d"), Color.parseColor("#86efac"))
            pxkm >= DiDiAccessibilityService.EXCEPTION_MIN && isLong ->
                Triple("~ Largo, evalúa", Color.parseColor("#713f12"), Color.parseColor("#fbbf24"))
            isLong -> Triple("✗ Rechaza", Color.parseColor("#7f1d1d"), Color.parseColor("#fca5a5"))
            else -> Triple("✗ Rechaza", Color.parseColor("#7f1d1d"), Color.parseColor("#fca5a5"))
        }

        tvVerdict.text = verdictText
        tvVerdict.setTextColor(textColor)
        overlayView.findViewById<View>(R.id.verdict_bg).setBackgroundColor(bgColor)

        val km1Str = String.format("%.1f", kmPickup)
        val km2Str = String.format("%.1f", kmTrip)
        val totalStr = String.format("%.1f", totalKm)
        val priceStr = price.roundToInt().toString()
            .reversed().chunked(3).joinToString(".").reversed()

        tvDetails.text = "${km1Str}+${km2Str}=${totalStr}km · \$$priceStr"

        if (!isCollapsed) {
            contentGroup.visibility = View.VISIBLE
            tvWaiting.visibility = View.GONE
        }

        // Tamaño del precio según veredicto
        tvPxkm.setTextSize(TypedValue.COMPLEX_UNIT_SP,
            if (pxkm >= DiDiAccessibilityService.MIN_PRICE_PER_KM) 32f else 28f)
    }

    private fun showWaiting() {
        if (!isCollapsed) {
            contentGroup.visibility = View.GONE
            tvWaiting.visibility = View.VISIBLE
        }
    }

    override fun onDestroy() {
        super.onDestroy()
        unregisterReceiver(updateReceiver)
        if (::overlayView.isInitialized) {
            windowManager.removeView(overlayView)
        }
    }

    override fun onBind(intent: Intent?): IBinder? = null

    private fun createNotificationChannel() {
        val channel = NotificationChannel(
            CHANNEL_ID, "DiDi Calc Overlay",
            NotificationManager.IMPORTANCE_LOW
        ).apply { description = "Servicio activo en segundo plano" }
        val nm = getSystemService(NotificationManager::class.java)
        nm.createNotificationChannel(channel)
    }

    private fun buildNotification(): Notification =
        NotificationCompat.Builder(this, CHANNEL_ID)
            .setContentTitle("DiDi Calc activo")
            .setContentText("Calculando precio/km en tiempo real")
            .setSmallIcon(android.R.drawable.ic_menu_compass)
            .setPriority(NotificationCompat.PRIORITY_LOW)
            .build()

    private fun dpToPx(dp: Int): Int =
        TypedValue.applyDimension(
            TypedValue.COMPLEX_UNIT_DIP, dp.toFloat(),
            resources.displayMetrics
        ).toInt()
}
